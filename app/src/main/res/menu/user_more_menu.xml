<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:id="@+id/group_operations">
        <item
            android:id="@+id/action_add_user"
            android:title="Add User" />
    </group>
    <group android:id="@+id/group_settings">
        <item
            android:id="@+id/action_send_settings"
            android:title="Send Settings (Replace All)" />
    </group>
    <group android:id="@+id/group_users">
        <item
            android:id="@+id/action_send_users_replace_all"
            android:title="Send Users (Replace All)" />
        <item
            android:id="@+id/action_send_users_add_only"
            android:title="Send Users (Add New Only)" />
    </group>
</menu>
