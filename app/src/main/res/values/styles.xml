<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Style for a small circular ProgressBar -->
    <style name="CircularProgressBar" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/circular_progress_bar</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:minHeight">80dp</item>
        <item name="android:maxHeight">80dp</item>
    </style>
</resources>